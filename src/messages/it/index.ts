import type { Messages } from "../types";
import { labels } from "./labels";

export const it = {
  common: {
    someone: "Qualcuno",
    justNow: "proprio ora",
  },
  labels,
} satisfies Messages;
